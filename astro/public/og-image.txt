This is a placeholder for og-image.png.

To generate the real OG image, you'll need to:

1. Use the provided og-image.svg as the source
2. Convert it to a 1200x630px PNG file
3. Optimize it for sharing on social media

You can use tools like:
- Inkscape: inkscape -z -w 1200 -h 630 og-image.svg -o og-image.png
- ImageMagick: convert -background none -resize 1200x630 og-image.svg og-image.png
- Online converters: https://svgtopng.com/

Once generated, replace this file with the actual og-image.png.